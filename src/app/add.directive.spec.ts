import { AddDirective } from './add.directive';

describe('AddDirective', () => {
  it('should create an instance', () => {
    const directive = new AddDirective();
    expect(directive).toBeTruthy();
  });
});
