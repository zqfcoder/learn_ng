import { HlDirective } from './hl.directive';

describe('HlDirective', () => {
  it('should create an instance', () => {
    const directive = new HlDirective();
    expect(directive).toBeTruthy();
  });
});
